#  Flutter API List Demo – Technical Assessment


Flutter app for the VSI technical assessment. List, search, and detail over public APIs.

**Configuration:** App shell + theme (`styles/`), reusable widgets (`core/widgets/`), responsive layout (`core/layout/`), Iconsax.

**Run:**

```bash
flutter pub get
flutter run
```
