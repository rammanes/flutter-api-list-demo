import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Theme (cream + blue palette)
  static const Color seed = Color(0xFF5AB2FF);
  static const Color primary = Color(0xFF5AB2FF);
  static const Color secondary = Color(0xFFA0DEFF);
  static const Color appBarBackground = Color(0xFFCAF4FF);
  static const Color appBarForeground = Color(0xFF1A5F8A);
  /// Use for text/icons on primary (e.g. splash).
  static const Color onPrimary = Colors.white;
  static const Color error = Color(0xFFB00020);
  static const Color scaffoldBackground = Color(0xFFFFFEF8);
  static const Color surfaceLight = Color(0xFFCAF4FF);
}